# Copy this file to .env and fill in your values
# .env is gitignored and never committed

# REQUIRED: Set a strong secret key for API authentication
API_KEY=changeme

# Optional overrides
DEFAULT_LOCALE=en_US
MAX_ROWS=10000
